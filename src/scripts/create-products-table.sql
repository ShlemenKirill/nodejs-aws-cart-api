CREATE TABLE products (
                          id UUID PRIMARY KEY,
                          title varchar(255),
                          description text,
                          price NUMERIC(10, 2)
);
